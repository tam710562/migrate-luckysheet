export interface ISheetRightClickConfig {   
    delete: boolean, //Delete
    copy: boolean, //Copy
    rename: boolean, //Rename
    color: boolean, //Change color
    hide: boolean, //Hide, unhide
    move: boolean, //Move to the left, move to the right
}

